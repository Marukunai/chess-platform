package com.chessplatform.achievement;

/** Puramente para agrupar visualmente en el cliente — no afecta a cómo se calcula el progreso. */
public enum AchievementCategory {
    GENERAL, VICTORIAS, RATING, MODALIDADES, SOCIAL, PERFIL, BOTS, PUZZLES
}